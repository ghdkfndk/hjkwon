# AGENTS.md

## Cursor Cloud specific instructions

This repository (`hjkwon`) is currently empty — it contains only a `README.md` file. There are no applications, services, dependencies, tests, or build steps to run.

- **No package manager lockfile** exists, so no dependency installation is needed.
- **No linting, testing, or build commands** are configured.
- **No services** need to be started.

When application code is added to this repository, update this section with:
- How to install dependencies
- How to run lint/test/build
- How to start development services
- Any non-obvious setup caveats
